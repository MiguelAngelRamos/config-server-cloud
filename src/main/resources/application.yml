server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MiguelAngelRamos/config-server-msv
          default-label: main # Rama predeterminada
